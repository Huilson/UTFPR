<html xmlns:th="http://www.thymeleaf.org">
    <head>
        <title>Visualizar Aluno</title>
        <meta charset="UTF-8"/>
        <!--Import Google Icon Font-->
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet"/>
        <!--Import materialize.css-->
        <link type="text/css" rel="stylesheet" href="../../css/materialize.min.css"  media="screen,projection"/>
        <!--Let browser know website is optimized for mobile-->
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    </head>

    <body class="grey lighten-2">
        <div id="FichaDeAluno" class="container">
            <h3 class="main-title center">Ficha do Aluno</h3>
            <div class="row">
                <div class="section">
                    <h5>Dados do Aluno</h5>
                    <div class="row">
                        <div class="input-field col s12">
                            <input id="nome" type="text" th:field="${aluno.nome}" disabled="disabled"/>
                            <label for="nome">Nome</label>
                        </div>
                        <div class="input-field col s12">
                            <input id="idade" type="date" th:field="${aluno.idade}" disabled="disabled"/>
                            <label for="idade">Idade</label>
                        </div>
                        <div class="input-field col s12">
                            <input id="curso" type="text" th:field="${aluno.curso}" disabled="disabled"/>
                            <label for="curso">Curso</label>
                        </div>
                    </div>
                </div>
                <div class="section">
                    <h5>Dados da Disciplinas do Aluno</h5>
                    <div class="row">
                        <ul>
                            <th:block th:object="${aluno}">
                                <li th:each="disciplina : *{disciplinas}" th:text="${'Disciplinas: ' + disciplina.nome
                                    + ' Notas: '+ disciplina.notas}"></li>
                            </th:block>
                        </ul>
                    </div>
                </div>
                <div class="row">
                    <div class="input-field col s12 center">
                        <a th:href="@{../listar}" title="Voltar">
                            <i class="medium material-icons">arrow_back</i>
                        </a>
                        <a  th:href="@{'/aluno/atualizar/'+${aluno.id}}" title="Editar Aluno">
                            <i class="medium material-icons">sync</i>
                        </a>
                        <a th:href="@{'/aluno/excluir/'+${aluno.id}}" title="Excluir Aluno">
                            <i class="medium material-icons">delete_forever</i>
                        </a>
                    </div>
                </div>
            </div>
        </div>
        <script type="text/javascript" src="../../js/materialize.min.js"></script>
    </body>
</html>